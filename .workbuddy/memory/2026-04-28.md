# 2026-04-28 工作日志

## 历史按钮位置优化

- `historyBtnPosition` 默认值从固定 `120` 改为屏幕垂直中间 ± 60px 随机浮动
- `DraggableHistoryPanel` 添加 `initialY` prop，每次面板打开时跟随按钮当前 Y 坐标弹出
- App.tsx 传入 `initialY={historyBtnPosition}`，确保面板与按钮对齐，且不遮挡按钮（x=85）

## 生图历史面板位置策略（二次修正，2026-04-28）

- 面板位置不随按钮变动，`DEFAULT_Y = window.innerHeight/2 - 240`（固定垂直居中偏上）
- 去掉了每次 open 时同步按钮位置的 effect，用户拖动后位置保持稳定

## 生图历史布局重构（2026-04-28）

- 移除浮动 `DraggableHistoryPanel` 组件，改为内嵌左侧固定栏
- 主体区域改为 `flex gap-3` 布局：左侧历史栏（w-[300px]，可折叠）+ 右侧生成结果（flex-1 自适应）
- 历史按钮保持 fixed 浮动，功能不变（切换 `historyPanelOpen`）
- `DraggableHistoryPanel.tsx` 保留但不再引用（可后续清理）
- 按钮文字始终显示"历史"（去掉"关闭历史"），图标 `w-3.5 h-3.5`

## 生图历史排版切换（2026-04-28）

- 状态 `historyLayout: "list" | "grid"`，默认列表视图
- 头部加入切换按钮：列表≡ / 网格田 图标按钮组
- 列表视图：横向缩略图 + 文字信息（原有样式）
- 网格视图：2列正方形网格，悬停显示渐变遮罩和文字信息
- 批量模式下网格视图左上角显示选择checkbox

## 生图历史交互优化（2026-04-28）

- 左侧"历史"按钮：更小（px-2.5 py-1.5），图标更小（w-2.5 h-2.5），透明度增加（`/80` + backdrop-blur）
- 点击历史条目：**不再填入提示词**，只做查看（批量模式仍可多选）
- 双击缩略图：弹出 `HistoryFullPreview` 大图预览（列表视图+网格视图均支持）





# 2026-04-27 工作日志

## 项目全面检测（文生图应用）

### Bug（高优先级）
- **B1** ImagePreviewModal.tsx:88 `forceUpdate++` 应改为 `forceUpdate((v) => v + 1)`
- **B5** balance.ts 读旧版配置 `getApiSettings()`，应改为 `getApiConfig().globalBaseUrl`
- **B6** testApiGenerate 的 Gemini body 格式过时（responseModalities + aspectRatio 放错位置）
- **B3** settingsForm 与 cfgDraft 双轨状态不同步（legacy 代码残留）
- **B4** 两个 useEffect 同时写 liang007_prompt_history，20条/50条不确定

### 功能缺口（高优先级）
- **F1** 生图无取消功能（AbortController 已有，缺少 UI 取消按钮）
- **F3** 历史记录全屏预览保存的是150px缩略图而非原图（originalUrl 字段未使用）
- **F7** 余额查询接口适配新配置结构

### 其他发现
- HistoryFullPreview 拖拽每帧 setState，性能差（应参考 ImagePreviewModal 改用 ref）
- 比例不匹配弹窗重新生成无防重入保护
- uiStore 中 promptPreviewOpen/imageEditorUrl 等已预留状态但未实现对应功能
- 历史面板无虚拟化（50条时有性能问题）

## 全量修复（2026-04-27 上午）

### 已修复（11 项，0 lint 错误）
| 编号 | 文件 | 修复内容 |
|------|------|---------|
| B1 | ImagePreviewModal.tsx | `forceUpdate++` → `forceUpdate(v=>v+1)` |
| B2 | HistoryFullPreview.tsx | 拖拽从 setState 改为 ref 追踪，mouseUp 才写 state；添加 ESC 关闭 |
| B3 | App.tsx | 模型管理弹窗"完成"按钮同步 settingsForm 到 cfgDraft.imageModels |
| B4 | App.tsx | 启动 cleanup effect 从限制 20 条改为 50 条，消除双写冲突 |
| B5 | balance.ts | 改用 `getApiConfig()` + `getActiveImageModel()` 读正确配置 |
| B6 | imageClient.ts | testApiGenerate Gemini body：删除 responseModalities，aspectRatio 移到 imageConfig 内 |
| B7 | App.tsx | 历史条目失败时显示红色边框/背景；进行中显示淡灰色 |
| F3 | App.tsx | 一键应用历史时 originalUrl 无条件恢复（去掉了 startsWith blob/data 的错误条件） |
| I3 | imageUtils.ts | createThumbnail 改为静默降级（所有错误 resolve("") 而非 reject） |
| I4 | download.ts | downloadImage 加 30s AbortController 超时保护 |
| I7 | App.tsx | 比例不匹配弹窗加 ratioMismatchRetried ref，重试后不再循环弹窗 |

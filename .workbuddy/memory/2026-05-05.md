# 2026-05-05

## 主题重设计收尾（Task#15 + Task#16）

### Task#15：顶部工具栏按钮统一
- **关于** 按钮：移除 `bg-gradient-to-r from-green-500 to-emerald-500` 硬编码渐变，改为 `glass-button`
- **性能监控** 按钮：移除 `bg-gradient-to-r from-cyan-500 to-blue-500` 渐变，改为 `glass-button`，激活态保留 `ring-2 ring-white/30`
- 文件：`client/src/App.tsx` ~第 953–971 行

### Task#16：主题菜单优化
- 宽度 `w-40` → `w-52`（避免长名截断）
- 每项左侧新增 16×16 色块圆点（`background: t.dotGradient`），直接用 theme.ts 的 `dotGradient` 字段
- 移除了多余的 `colorMap` hardcode 对象
- 文件：`client/src/App.tsx` ~第 979–1008 行

### 编译状态
- `tsc --noEmit` 0 错误
- 开发服务器运行正常（localhost:5173）

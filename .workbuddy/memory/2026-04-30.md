# 2026-04-30

## AspectRatioSelect 修复

### 问题
1. 比例下拉被 `<aside overflow-hidden>` 遮挡 → 改为 `position: fixed` + `getBoundingClientRect()`
2. 图标比例不对 → 改为 `RatioRect` 按实际比例计算精确宽高
3. "原比例"换行导致下拉失效 + 宽度不足
   - `RIGHT_PANEL_DEFAULT`: 360 → 340
   - 网格列宽: `grid-cols-4` → `grid-cols-[1fr_88px_68px_1fr]`
   - 触发按钮: 加 `whitespace-nowrap overflow-hidden`
   - 文字 span: 加 `overflow-hidden text-ellipsis`

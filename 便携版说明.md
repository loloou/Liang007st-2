# liang007生图 - 便携版

## 已完成打包

应用已打包为**便携版**，位置：

```
dist-electron\liang007生图-win32-x64\liang007生图.exe
```

## 使用方式

这是一个**便携版应用**，包含 exe 和所有依赖文件。

**分发给他人使用：**
- 直接将整个文件夹 `liang007生图-win32-x64` 压缩成 zip 文件
- 对方解压后双击 `liang007生图.exe` 即可运行

**自己使用：**
- 将文件夹移动到任何位置
- 双击 `liang007生图.exe` 运行

## 目录结构

```
liang007生图-win32-x64/
├── liang007生图.exe      # 主程序
├── resources/            # 应用资源
├── locales/             # 多语言文件
└── *.dll                # 运行时依赖
```

## 说明

由于 Electron 应用的特性（需要 Chromium 运行时、多语言资源等），
目前无法打包成单个 exe 文件。目录模式是 Electron 官方推荐的便携方式。
